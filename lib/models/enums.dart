enum UserRole {
  admin,
  subcontractor,
}

enum ApprovalStatus {
  pending,
  approved,
  rejected,
}

enum AttendanceStatus {
  present,
  absent,
  notMarked,
}

enum DeploymentStatus {
  deployed,
  nonDeployed,
  underRepair,
}
